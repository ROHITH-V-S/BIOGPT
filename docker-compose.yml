services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    env_file: .env
    depends_on: [postgres, redis]
    volumes:
      - backend_data:/app/data
  
  frontend:
    build: ./frontend
    ports: ["3000:80"]
    depends_on: [backend]
  
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: biogpt
      POSTGRES_USER: biogpt
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-biogpt_dev}
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
  
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    volumes:
      - redis_data:/data

volumes:
  pgdata:
  redis_data:
  backend_data:
