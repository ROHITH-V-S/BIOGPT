fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
openai==1.50.0
python-dotenv==1.0.1
tenacity>=8.1.0,<9.0.0

numpy==1.26.4
faiss-cpu==1.9.0
sentence-transformers==3.0.0
langchain==0.3.0
langchain-text-splitters==0.3.0
PyPDF2==3.0.1
requests==2.32.0
sse-starlette==2.1.0
httpx==0.27.0
tabulate==0.9.0        # eval + comparison report tables
redis==5.0.8           # optional cache + rate-limit store; app runs without it
spacy>=3.7.0
# The biomedical NER model is NOT pip-installable on Python 3.12+: its
# setup.py needs Cython at build time, and the `scispacy` package pins nmslib,
# which has no modern wheels. Install the model with:
#     python scripts/install_ner_model.py
# Without it the app still runs — app/ner.py falls back to regex extraction.

pytest==8.3.0
pytest-asyncio==0.24.0
